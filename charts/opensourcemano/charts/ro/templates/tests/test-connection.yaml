apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ro.fullname" . }}-test-connection"
  labels:
    {{- include "ro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
