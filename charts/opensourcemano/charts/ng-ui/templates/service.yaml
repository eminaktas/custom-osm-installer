apiVersion: v1
kind: Service
metadata:
  name: {{ include "ng-ui.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ng-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{ if (and (eq .Values.service.type "NodePort") (not (empty .Values.service.nodePort))) }}
      nodePort: {{.Values.service.nodePort}}
      {{ end }}
  selector:
    {{- include "ng-ui.selectorLabels" . | nindent 4 }}
