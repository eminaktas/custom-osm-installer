apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ng-ui.fullname" . }}-test-connection"
  labels:
    {{- include "ng-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ng-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
