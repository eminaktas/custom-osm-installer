apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mon.fullname" . }}-test-connection"
  labels:
    {{- include "mon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
