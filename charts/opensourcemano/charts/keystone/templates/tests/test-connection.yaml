apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keystone.fullname" . }}-test-connection"
  labels:
    {{- include "keystone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keystone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
